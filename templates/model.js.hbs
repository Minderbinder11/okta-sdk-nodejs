/**
 *  THIS FILE IS AUTO GENERATED - SEE CONTRIBUTOR DOCUMENTATION
 */

/**
 * @class {{modelName}}
 */
class {{modelName}} {
  constructor(resourceJson, client) {
    Object.assign(this, resourceJson);
    this.client = client;
  }

  {{#each methods}}
  {{alias}}({{modelMethodPublicArgumentBuilder this}}) {
    return this.client.{{operation.operationId}}({{modelMethodProxyArgumentBuilder this}});
  }
  {{/each}}

  delete() {
    return this.client.http.http(this._links.self.href, {
      method: 'delete'
    });
  }
}

module.exports = {{modelName}};